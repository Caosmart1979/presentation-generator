# Presentation Generator

> AI 驱动的高质量 PPT 生成器 - 使用 Gemini + GLM-4.7

## 简介

Presentation Generator 是一个符合 MindSymphony 规范的 PPT 生成技能，能够：

- 📄 **智能分析文档**，自动规划 PPT 结构
- 🎨 **使用 Gemini 生成高质量图片**
- 🤖 **使用 GLM-4.7 生成转场描述**（可选）
- 🎮 **提供交互式 Web 播放器**

## 快速开始

### 1. 配置 API 密钥

```bash
cd ~/.claude/skills/presentation-generator
cp .env.example .env
```

编辑 `.env` 文件，填入你的 API 密钥：

```bash
# Gemini API (必需)
GEMINI_API_KEY=your_gemini_api_key_here

# GLM API (可选)
GLM_API_KEY=your_glm_api_key_here
```

### 2. 安装依赖

```bash
pip install -r requirements.txt
```

### 3. 使用

在 Claude Code 中直接说：

```
调用 presentation-generator 为以下内容生成 5 页 PPT：
人工智能的未来发展与应用
```

## 使用方式

### 方式一：通过 Claude Code

```
生成一个关于量子计算的 5 页 PPT，使用渐变毛玻璃风格
```

### 方式二：直接调用 Python

```python
from generators.ppt_generator import PPTGenerator

generator = PPTGenerator()
result = generator.generate(
    content="人工智能的未来",
    page_count=5,
    style="gradient-glass",
    resolution="2K"
)
```

## 风格库

| 风格 | 文件 | 特点 |
|------|------|------|
| 渐变毛玻璃 | `gradient-glass.md` | 霓虹渐变、玻璃拟态 |
| 矢量插画 | `vector-illustration.md` | 扁平矢量、可爱风格 |

自定义风格：在 `config/styles/` 创建新的 `.md` 文件

## 输出结构

```
outputs/20240113_120000/
├── images/                  # 生成的图片
│   ├── slide_01_cover.png
│   ├── slide_02_content.png
│   └── ...
├── viewer.html              # 播放器
├── slides_plan.json         # 内容规划
├── prompts.json             # 生成提示词
└── generation_log.json      # 生成日志
```

## 键盘控制

| 按键 | 功能 |
|------|------|
| `→` `↓` | 下一页 |
| `←` `↑` | 上一页 |
| `Home` | 首页 |
| `End` | 末页 |
| `F` | 全屏 |
| `H` | 隐藏提示 |

## 与 MindSymphony 集成

该技能完全符合 MindSymphony v19.1 规范，可以与以下技能协同：

- `gemini-cli-integration` - 复用 Gemini 调用逻辑
- `b-01-prompt-pharmacist` - 协同优化提示词
- `d-06-visual-poet` - 协同设计视觉风格

## 技术架构

```
┌─────────────────────────────────────────────────────────────┐
│                    Presentation Generator                     │
├─────────────────────────────────────────────────────────────┤
│  1. 文档分析 → PPTGenerator._generate_slides_plan()         │
│  2. 风格加载 → StyleManager.load_style()                    │
│  3. 提示词生成 → PromptGenerator.generate_prompts()          │
│  4. Gemini 生成图片 → GeminiClient.generate_slides()         │
│  5. GLM-4.7 转场描述 → GLMClient.generate_transition()       │
│  6. 播放器生成 → PPTGenerator._generate_viewer()             │
└─────────────────────────────────────────────────────────────┘
```

## 常见问题

### Q: 必须配置 GLM API 吗？

A: 不是必须的。GLM-4.7 用于生成转场描述和内容优化，如果未配置，系统会使用默认值。

### Q: 支持哪些分辨率？

A: 支持 2K (2752x1536) 和 4K (5504x3072)。

### Q: 可以自定义风格吗？

A: 可以，在 `config/styles/` 创建新的 `.md` 文件，参考现有风格格式。

### Q: 生成的图片可以商用吗？

A: 请查阅 Google Gemini 的使用条款。

## 项目结构

```
presentation-generator/
├── SKILL.md                  # 技能入口
├── README.md                 # 本文件
├── .env.example              # 环境变量模板
├── requirements.txt          # Python 依赖
├── core/                     # 核心模块
│   ├── api_adapter.py
│   ├── gemini_client.py
│   ├── glm_client.py
│   └── style_manager.py
├── generators/               # 生成器
│   ├── ppt_generator.py
│   └── prompt_generator.py
├── config/                   # 配置
│   ├── styles/               # 风格库
│   └── prompts/              # 提示词模板
├── templates/                # 模板
│   └── viewer.html
├── agents/                   # Subagent
│   └── ppt-executor.md
└── references/               # 参考文档
```

## 版本历史

### v1.0.0 (2024-01-13)
- 初始版本
- 支持 Gemini 图片生成
- 支持 GLM-4.7 辅助功能
- Web 播放器
- 2 种内置风格

## License

MIT License
